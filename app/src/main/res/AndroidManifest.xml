<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Nothing Samsung-Health-specific is needed here - verified against the official
      "Health Diary" sample app, whose manifest also has no SDK entries.

      samsung-health-data-api-1.1.0.aar ships its own manifest, which the merger adds:
        <queries><package android:name="com.sec.android.app.shealth" />
                 <package android:name="com.samsung.android.wear.shealth" /></queries>
        <uses-permission android:name="android.permission.INTERNET" />
        <uses-sdk android:minSdkVersion="29" />

      Health data consent is granted at runtime through HealthDataStore.requestPermissions().
      The old "com.samsung.android.health.permission.read" meta-data belongs to the previous
      Samsung Health SDK for Android and must NOT be used with the Data SDK.
    -->

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
